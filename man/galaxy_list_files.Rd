% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2026-01-21_s4_class_methods.R
\name{galaxy_list_files}
\alias{galaxy_list_files}
\alias{galaxy_list_files,character-method}
\alias{galaxy_list_files,Galaxy-method}
\title{List all files (datasets) in a Galaxy history}
\usage{
galaxy_list_files(
  x,
  include_deleted = FALSE,
  include_hidden = FALSE,
  galaxy_url = "https://usegalaxy.eu",
  limit = 500L
)

\S4method{galaxy_list_files}{character}(
  x,
  include_deleted = FALSE,
  include_hidden = FALSE,
  galaxy_url = "https://usegalaxy.eu",
  limit = 500L
)

\S4method{galaxy_list_files}{Galaxy}(
  x,
  include_deleted = FALSE,
  include_hidden = FALSE,
  limit = 500L
)
}
\arguments{
\item{x}{A history ID (\code{character}) or a \code{Galaxy} object.}

\item{include_deleted}{Logical; if \code{TRUE}, include deleted datasets.
Default: \code{FALSE}.}

\item{include_hidden}{Logical; if \code{TRUE}, include hidden datasets.
Default: \code{FALSE}.}

\item{galaxy_url}{Character. Base URL of the Galaxy instance, used by the
character method. If \code{GALAXY_URL} is set, it takes precedence.}

\item{limit}{Integer page size for API pagination. Default: \code{500L}.}
}
\value{
A \code{data.frame} with one row per dataset and columns:
\describe{
\item{id}{Encoded dataset ID.}
\item{name}{Dataset name.}
\item{history_id}{Encoded history ID.}
\item{history_content_type}{Usually \code{"dataset"}.}
\item{type}{Galaxy item type (if provided by server).}
\item{state}{Dataset state (e.g. \code{"ok"}, \code{"running"}, \code{"error"}).}
\item{deleted}{Logical deletion flag.}
\item{hidden}{Logical hidden flag.}
\item{file_ext}{Datatype extension (if available).}
\item{file_size}{Dataset size in bytes (if available).}
\item{create_time}{Creation timestamp (if available).}
\item{update_time}{Update timestamp (if available).}
}

If no datasets are found, an empty data frame with these columns is returned.
}
\description{
\code{galaxy_list_files()} is an S4 generic. With \code{x} as a character scalar,
it is treated as a Galaxy history ID and returns a data frame of history
datasets (files). With \code{x} as a \code{Galaxy} object, the history ID and URL
are taken from the object.
}
\details{
The function queries \verb{/api/histories/\{history_id\}/contents} using
pagination and returns one row per history content item, restricted to
datasets (\code{history_content_type == "dataset"} when available).
}
\examples{
\dontshow{if (galaxy_has_key()) withAutoprint(\{ # examplesIf}
# Character method (history_id)
hid <- galaxy_initialize("List files example")
files_df <- galaxy_list_files(hid)
head(files_df)

# Galaxy-object method
g <- galaxy(history_name = "List files example")
g <- galaxy_initialize(g)
files_df2 <- galaxy_list_files(g)
head(files_df2)
\dontshow{\}) # examplesIf}
}
