% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2026-01-21_s4_class_methods.R
\name{galaxy_delete_history}
\alias{galaxy_delete_history}
\alias{galaxy_delete_history,character-method}
\alias{galaxy_delete_history,Galaxy-method}
\title{Generic for deleting a Galaxy history}
\usage{
galaxy_delete_history(
  x,
  purge = FALSE,
  galaxy_url = "https://usegalaxy.eu",
  verbose = FALSE
)

\S4method{galaxy_delete_history}{character}(
  x,
  purge = FALSE,
  galaxy_url = "https://usegalaxy.eu",
  verbose = FALSE
)

\S4method{galaxy_delete_history}{Galaxy}(x, purge = FALSE, verbose = FALSE)
}
\arguments{
\item{x}{A history ID (\code{character}) or a \code{Galaxy} object.}

\item{purge}{Logical. If \code{TRUE}, request permanent deletion (purge).
Default: \code{FALSE}.}

\item{galaxy_url}{Character. Base URL of the Galaxy instance, used by the
character method. If \code{GALAXY_URL} is set, it takes precedence.}

\item{verbose}{Logical. If \code{TRUE}, print request status. Default: \code{FALSE}.}
}
\value{
\itemize{
\item For the \code{character} method: a named list with \code{success}, \code{status}, \code{content}.
\item For the \code{Galaxy} method: the modified \code{Galaxy} object (state set to
\code{"success"} on 2xx, otherwise \code{"error"}).
}
}
\description{
\code{galaxy_delete_history()} is an S4 generic. With \code{x} as a character scalar,
it is treated as a history ID and the function returns API response metadata.
With \code{x} as a \code{Galaxy} object, \code{history_id} and \code{galaxy_url} are taken from
the object and the object state is updated.
}
\details{
The function calls \code{DELETE /api/histories/{history_id}} with optional
\code{purge=TRUE} to request permanent removal (server policy permitting).
}
\examples{
\dontshow{if (galaxy_has_key()) withAutoprint(\{ # examplesIf}
\dontrun{
# Character method
hid <- galaxy_initialize("history to delete")
galaxy_delete_history(hid, purge = FALSE)

# Galaxy method
g <- galaxy(history_name = "history to delete")
g <- galaxy_initialize(g)
g <- galaxy_delete_history(g, purge = FALSE)
}
\dontshow{\}) # examplesIf}
}
